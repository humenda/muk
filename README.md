# needs Cargo and rustup with the src component enabled
rustup component add rust-src
# sudo apt install lld clang 
ToDo: ~/.icargo/config
# cargo install xargo
sudo apt install clang llvm lld qemu-system-arm
